$primary = hsl(219, 88%, 55%);
$danger = hsl(0, 88%, 55%);
$success = hsl(115, 57%, 51%);
$bg = hsl(0, 0%, 98%);
$bg-dark = hsl(240, 5%, 7%);

:root {
    --primary: $primary;
    --danger: $danger;
    --success: $success;
    --ui-radius: 0.6rem;
}